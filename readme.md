# PON! SCORE

横向き画面で使う卓球スコアボードです。音声ファイルを読み込むため、`index.html` の直接表示ではなくHTTPサーバーで配信してください。

## 起動例

展開したフォルダを静的Webサーバーで公開し、ブラウザから `index.html` を開きます。

## 主な操作

- 左右の＋／−で得点を変更
- 右上の「設定」から選手名、試合形式、最初のサーブ、表示テーマ、左右表示を変更
- ライト／ダークの選択はブラウザに保存

## 補足

この配布物はビルド済みの静的ファイルです。`file://` での直接起動では音声が再生されない場合があります。
